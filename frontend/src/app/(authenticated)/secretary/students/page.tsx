'use client';
// The secretary's student management screens reuse the admin screens.
// Breadcrumb prefix differs but the page logic is shared via component composition.
export { default } from '@/app/(authenticated)/admin/students/page';
